/**
 * Auto-generated by SmartBank AI Codegen
 * Source: codegen/schemas/user.schema.yaml
 */

const Joi = require('joi');

const registerUserSchema = Joi.object({
  user_id: Joi.string().optional(),
  name: Joi.string().required().min(2).max(100),
  email: Joi.string().required().email(),
  role: Joi.string().optional().valid('ADMIN', 'USER'),
  salary: Joi.number().optional().min(0),
  profileType: Joi.string().optional().valid('Student', 'Working Professional', 'Self Employed', 'Business Owner', 'Other'),
  isEmailVerified: Joi.boolean().optional(),
  password: Joi.string().min(8).required(),
});

const loginUserSchema = Joi.object({
  email: Joi.string().email().required(),
  password: Joi.string().required(),
});

const updateUserSchema = Joi.object({
  user_id: Joi.string().optional(),
  name: Joi.string().optional().min(2).max(100),
  email: Joi.string().optional().email(),
  role: Joi.string().optional().valid('ADMIN', 'USER'),
  salary: Joi.number().optional().min(0),
  profileType: Joi.string().optional().valid('Student', 'Working Professional', 'Self Employed', 'Business Owner', 'Other'),
  isEmailVerified: Joi.boolean().optional(),
}).min(1); // at least one field must be provided

const validate = (data, schema) => schema.validate(data, { abortEarly: false });

module.exports = { registerUserSchema, loginUserSchema, updateUserSchema, validate };
